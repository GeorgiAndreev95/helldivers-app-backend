import Faction from "../models/Faction.js";

export const createFaction = (req, res, next) => {
    const { name, description, image } = req.body;
};

export const getFaction = (req, res, next) => {};

export const getFactions = (req, res, next) => {};

export const updateFaction = (req, res, next) => {};

export const deleteFaction = (req, res, next) => {};
